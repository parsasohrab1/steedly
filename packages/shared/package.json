{
  "name": "@asbaan/shared",
  "version": "0.1.0",
  "description": "Shared types and the multi-disease risk-scoring engine (colic / laminitis / azoturia) used by the Asbaan backend, web and mobile apps",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
